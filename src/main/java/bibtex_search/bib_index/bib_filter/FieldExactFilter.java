package bibtex_search.bib_index.bib_filter;

/**
 * An exemplary filter that discards all the records where given field
 * does not have exactly the specified value.
 *
 * It can be altered in a multitude of ways - instead of an exact match it could be e.g. an inclusion
 * of a number in a given interval - then instead of a simple map it should keep an interval tree for example.
 */
public class FieldExactFilter {
}
